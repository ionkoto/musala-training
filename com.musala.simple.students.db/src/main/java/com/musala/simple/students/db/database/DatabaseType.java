package com.musala.simple.students.db.database;

/**
 * Database types (names) for the different implementations
 * of the {@link AbstractDatabase}
 */
public enum DatabaseType {
    MongoDb,
    MySQL
}
